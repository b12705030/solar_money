# Next.js
.next/
out/
tsconfig.tsbuildinfo

# Dependencies
node_modules/

# Python
.venv/
__pycache__/
*.pyc

# Environment variables (secrets — commit the .example versions instead)
.env.local
backend/.env

# Misc
*.log
.DS_Store
solar-app.zip

# DEM 原始資料（721 MB，太大不進 repo）
data/不分幅_全台20MDEM*/
# 降采樣後的 .npy（28.9 MB）則刻意不排除，讓其他人 clone 即用

# ─── GBA 建物原始 tiles（~6 GB，由 HuggingFace 下載後手動整理）─────────────
# 下載/整理流程見 scripts/import_gba_to_db.py 說明
# 台灣裁切後的建物資料已匯入 Neon gba_buildings 表，不需這些原始檔
data/ODbLPolygon/
data/Polygon/
data/LoD1/
data/LoD1_GeoJSON/
# taiwan_polygon_fallback.ndjson.gz 透過 Git LFS 追蹤（執行 export_polygon_fallback.py 產生）
# 若尚未設定 LFS，此檔案也可放在本地 data/ 供 fallback 使用（不提交即可）
